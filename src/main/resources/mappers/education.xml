<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="education">
	
	
    
     <insert id="eduIns" parameterType="java.util.Map">
        INSERT INTO education(uid, eduno, eduop, edu_date, score)
        VALUES(#{uid},#{eduno},#{eduop},NOW(),#{score})
    </insert>
    
    
	<select id="eduList" resultType="java.util.Map">
		SELECT eduno, eduop, edu_date, score
		FROM education
		ORDER BY edu_date
	</select>
	
	
	
</mapper>