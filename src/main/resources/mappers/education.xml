<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="education">
	
	
    
    <insert id="insertQuizResults" parameterType="java.util.List">
        INSERT INTO quiz_sub(uid, question, user_answers, score, quiz_date)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.uid}, #{item.question}, #{item.userAnswers}, #{item.score}, NOW())
        </foreach>
    </insert>

</mapper>