<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="center">
 	<insert id="insert" parameterType="java.util.Map">
	 	INSERT INTO animal (anino, uid, aname, age, gender, weight, genop, vac, intro, anipic, adopt, adopt_pos)
		VALUES (#{anino}, 'kim9595', #{aname}, #{age}, #{gender}, #{weight}, #{genop}, #{vac}, #{intro}, #{anipic}, #{adopt}, #{adopt_pos})
 	</insert>
 	
 	
 	
 	<select id="form" resultType="java.util.Map">
 		SELECT anino, uid, aname, age, gender, weight, genop, vac, intro, anipic, adopt, cnt, adopt_pos
 		FROM animal
 	</select>
 	
 	<select id="detail" resultType="java.util.Map">
 		SELECT anino, uid, aname, age, gender, weight, genop, vac, intro, anipic, adopt, cnt, adopt_pos
 		FROM animal
 		WHERE anino = #{anino}
 	</select>
 	
 	
 </mapper>
 
 
	
	
	